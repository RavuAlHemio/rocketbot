*Usage:* `{cpfx}react [{lopfx}undo] MESSAGEID EMOJI`

Adds a reaction with the given `EMOJI` to the message with the given `MESSAGEID`.

If `-u`/`--undo` is given, removes an existing reaction by the bot instead.
