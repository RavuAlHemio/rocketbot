*Usage:* `{cpfx}pickglue TEXT`

Chooses an option from each line of `TEXT` and glues the result together.

Lines are delimited using newlines; options within a line are delimited using the pipe character `|`.
